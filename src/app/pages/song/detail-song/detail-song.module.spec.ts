import { DetailSongModule } from './detail-song.module';

describe('DetailSongModule', () => {
  const module: DetailSongModule = new DetailSongModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
