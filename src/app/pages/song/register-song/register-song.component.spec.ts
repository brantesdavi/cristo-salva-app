import { RegisterSongComponent } from './register-song.component';

describe('RegisterSongComponent', () => {
  let component: RegisterSongComponent;

  beforeEach(() => {
    component = new RegisterSongComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
